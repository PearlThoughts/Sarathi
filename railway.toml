[build]
builder = "RAILPACK"

[deploy]
preDeployCommand = "bun run knowledge migrate apply"
startCommand = "bun run teams:ingress"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
