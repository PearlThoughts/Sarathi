{
  "suites": [
    {
      "id": "teams-ingress",
      "path": "tests/teams-ingress.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Agents SDK ingress configuration fail-closed coverage."
    },
    {
      "id": "teams-mention",
      "path": "tests/teams-mention.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Direct mention, policy-gated answer orchestration, and duplicate-delivery coverage."
    },
    {
      "id": "workspace-model",
      "path": "tests/workspace-model.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Source inference and YAML overlay compilation coverage."
    },
    {
      "id": "platform-api",
      "path": "tests/platform-api.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Hono API surface coverage for the platform foundation."
    },
    {
      "id": "release-cli",
      "path": "tests/release-cli.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Runtime smoke command, Railway deploy guard, explicit synthetic operator workflows, and fail-closed runtime selection coverage."
    },
    {
      "id": "release-cli-file-backed",
      "path": "tests/release-cli-file-backed.bun.test.ts",
      "command": "bun run test:bun-native",
      "prerequisites": ["bun install"],
      "description": "File-backed workspace reconciliation, durable intent decisions, operator-supplied projection observations, sensitivity-scoped accountability counts, explicit report boundary context and denial handling, restart persistence, workspace isolation, local evidence import, watermarks, and private drift-review generation coverage."
    },
    {
      "id": "operator-runtime",
      "path": "tests/operator-runtime.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Explicit durable/synthetic runtime selector parsing and fail-closed ambiguity coverage."
    },
    {
      "id": "operator-runtime-sqlite",
      "path": "tests/operator-runtime.bun.test.ts",
      "command": "bun run test:bun-native",
      "prerequisites": ["bun install"],
      "description": "SQLite repository composition and exact workspace ID/key resolution coverage."
    },
    {
      "id": "boundary-policy",
      "path": "tests/boundary-policy.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Policy gate coverage for trust tier, delegation stage, approval, and model egress."
    },
    {
      "id": "follow-up",
      "path": "tests/follow-up.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Generic due-item planning and exception digest coverage."
    },
    {
      "id": "compliance-reminders",
      "path": "tests/compliance-reminders.bun.test.ts",
      "command": "bun run test:bun-native",
      "prerequisites": ["bun install"],
      "description": "Workspace-scoped dry-run, atomic idempotency reservation, delivery audit, and retry contract coverage."
    },
    {
      "id": "messaging",
      "path": "tests/messaging.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "External-principal mapping and proactive message delivery contract coverage."
    },
    {
      "id": "evidence-import-contract",
      "path": "tests/evidence-import-contract.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Read-only source adapter, normalized metadata, consent, hash, watermark, and idempotent import contract coverage."
    },
    {
      "id": "strategy-kernel",
      "path": "tests/strategy-kernel.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Strategy kernel contracts, visibility inheritance, migrations, and workspace pack reconciliation coverage."
    },
    {
      "id": "strategy-kernel-sqlite",
      "path": "tests/strategy-kernel-sqlite.bun.test.ts",
      "command": "bun run test:bun-native",
      "prerequisites": ["bun install"],
      "description": "Bun-native SQLite migration application and strategy-kernel repository persistence coverage."
    },
    {
      "id": "workspace-packs",
      "path": "tests/workspace-packs.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Workspace pack loading, YAML fragment reconciliation, and conflict behavior."
    },
    {
      "id": "intent-inbox",
      "path": "tests/intent-inbox.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Evidence ingestion, deterministic claim extraction, candidate inbox transitions, and audit events."
    },
    {
      "id": "projections",
      "path": "tests/projections.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Intended projection creation, observation classification, operator-observation persistence semantics, and projection drift findings."
    },
    {
      "id": "accountability-actions",
      "path": "tests/accountability-actions.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Non-live accountability action transitions, evidence-required completion, silence, escalation, and action-card feedback."
    },
    {
      "id": "strategic-reports",
      "path": "tests/strategic-reports.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Workspace-scoped strategic report generation, derived drift findings, sensitivity ceilings, and leakage guards."
    },
    {
      "id": "private-data-scan",
      "path": "tests/private-data-scan.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Tracked-file leakage scanning with invented stdin values and ignored private configuration."
    },
    {
      "id": "knowledge-layer",
      "path": "tests/knowledge-*.test.ts",
      "command": "bun run test",
      "prerequisites": ["bun install"],
      "description": "Canonical Jira/Vault knowledge and delivery projections, AI SDK embeddings, ACL-first hybrid retrieval, live GitHub fusion, concise cited answering, runtime composition, and durable CLI coverage."
    },
    {
      "id": "knowledge-postgres-integration",
      "path": "tests/knowledge-postgres.integration.test.ts",
      "command": "SARATHI_KNOWLEDGE_TEST_DATABASE_URL=<pgvector-postgres-url> bun run test:knowledge-postgres",
      "prerequisites": [
        "bun install",
        "PostgreSQL with pgvector available at the explicit test URL"
      ],
      "description": "Real PostgreSQL migration preservation, replay dedupe, edit versioning, ACL-first delivery query and finance separation, cross-workspace/sensitivity filtering, exact/FTS/vector retrieval, checkpoint, and deletion-tombstone coverage."
    },
    {
      "id": "delivery-intelligence-domain",
      "path": "tests/delivery-intelligence-domain.test.ts",
      "command": "bunx vitest run tests/delivery-intelligence-domain.test.ts",
      "prerequisites": ["bun install"],
      "description": "Delivery model, finance isolation, conflict derivation, safe query grammar, and optional time-constraint coverage."
    },
    {
      "id": "delivery-intelligence-application",
      "path": "tests/delivery-intelligence-application.test.ts",
      "command": "bunx vitest run tests/delivery-intelligence-application.test.ts",
      "prerequisites": ["bun install"],
      "description": "Bounded planning, pre-source finance authorization, workspace and sensitivity filtering, deduplication, compound briefs, active-over-terminal status ranking, conflicts, citations, and concise answer coverage."
    },
    {
      "id": "delivery-capability-matrix",
      "path": "tests/delivery-capability-matrix.test.ts",
      "command": "bunx vitest run tests/delivery-capability-matrix.test.ts tests/delivery-knowledge-query-source.test.ts",
      "prerequisites": ["bun install"],
      "description": "Reusable PM-question planning plus actor/workspace/audience/sensitivity propagation into bounded hybrid delivery retrieval."
    },
    {
      "id": "delivery-intelligence-live-sources",
      "path": "tests/delivery-query-sources.test.ts",
      "command": "bunx vitest run tests/delivery-query-sources.test.ts",
      "prerequisites": ["bun install"],
      "description": "Pre-provider access checks plus live GitHub, Jira, Teams channel-catalog routing, and scoped project-email delivery query coverage."
    },
    {
      "id": "teams-delivery-channel-projection",
      "path": "tests/teams-delivery-channel-projection.test.ts",
      "command": "bunx vitest run tests/teams-delivery-channel-projection.test.ts",
      "prerequisites": ["bun install"],
      "description": "Explicit bounded delivery-channel allowlists, ingress fallback, canonical labels and routing topics, shared/private scope declarations, and ambiguity rejection."
    },
    {
      "id": "delivery-cli",
      "path": "tests/delivery-cli.test.ts",
      "command": "bunx vitest run tests/delivery-cli.test.ts",
      "prerequisites": ["bun install"],
      "description": "Durable status/query/reconcile/rebuild routing, bounded request parsing, non-destructive rebuild, and privacy-safe failures."
    },
    {
      "id": "local-ci",
      "path": "package.json",
      "command": "bun run check",
      "prerequisites": ["bun install"],
      "description": "Repository-local CI equivalent."
    }
  ]
}
